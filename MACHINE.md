---
title: "MACHINE.md"
summary: "机器与软件环境事实——用于本机核对、路径调用与环境迁移"
importance: 5
platform: machine
created_at: 2026-08-22
read_when:
  - 使用任何本机路径、调用本机软件之前
  - 换电脑 / 换软件 / 重装之后
---

# MACHINE.md - 机器特化信息与迁移

> 本文件集中存放**只对当前这台电脑有效**的机器级信息，并索引**各软件环境**的特化文件。治理文件（SOUL / OPERATIONS / MEMORY 等）不硬编码此类信息，一律指向本文件或其下软件文件。
>
> 性质：整体为**参考补丁**（见 [FREEDOM.md](FREEDOM.md)）——迁移时不带走旧值，到新环境重新采集。
>
> 导航：跨平台 Markdown 与上下文处理的稳定原则见 [AGENT-CONTEXT.md](AGENT-CONTEXT.md)；本文件不复述该原则，只记录已核实的当前机器和软件事实。

## 1. 系统与用户（机器级）

- Windows 10 19045 x64；用户目录 `C:\Users\纳\`；当前工作环境 shell 为 PowerShell

## 2. 本机路径（机器级，2026-08-22 实测核对）

| 项 | 路径 | 备注 |
|---|---|---|
| 微信文件（最近活跃账号） | `C:\Users\纳\xwechat_files\wxid_b42pgs2r1dlg22_2427\msg\file` | 该账号 msg/file 最后活动 2026-08-02（本机实测）。另有旧账号 `wxid_apklc6yp8nw529_2a94`（2026-07-13 后无活动）。历史：2026-08-14 曾换机为 D 盘路径，现已换回本机 |
| 7-Zip | `C:\软件\7-Zip\7z.exe` | v22.00，装于非标准目录（教训 T22）；解压 rar/zip/7z 等直接调用，勿自行引导其他工具 |

## 3. 软件环境（按软件拆分；在哪个软件里，就只提该软件的文件）

**内容定位**：每个软件文件登记**该软件里会话用到的全部工具与调用功能**——包括上下文开头环境区告知的工具清单、调用约束、可用 skill——外加该软件的运行时坑。工具清单是快照，以每次会话开头告知为准对账。

**每个软件文件都须有「本软件在治理体系中的作用点（迁移清单）」一节**：它是换软件时的第一份待办清单——逐条登记"治理体系里的位置 → 本软件须提供什么 → 状态 / 依据"，并标出哪些是本软件**专有落点**（换软件时须逐处由新软件重新提供、重新核实，不照搬）。它是反向索引，只指位置、不复述机制。（缺口现状：`codebuddy.md` / `zcode.md` 缺「Agent 提示词注入适配器」与「官方机制 → 治理接口」，`codex.md` 缺「官方机制 → 治理接口」；连同上面的作用点节，三份均为**已知缺口**——按「迁移流程」第 2 条，提起该软件时先补。）

**规则：当前会话运行在哪个软件里，就只读取该软件的文件；其余软件的文件不加载。** 换环境＝换提哪个文件，不是全读。

| 软件 | 文件 | 何时提上来 |
|---|---|---|
| ZCode | [machine/zcode.md](machine/zcode.md) | 在 ZCode 会话里干活时 |
| WorkBuddy 桌面版 | [machine/workbuddy.md](machine/workbuddy.md) | 操作 / 排查 WorkBuddy 时 |
| Codex 桌面版 | [machine/codex.md](machine/codex.md) | 在 Codex 桌面版会话里干活时 |
| CodeBuddy Code（CLI） | [machine/codebuddy.md](machine/codebuddy.md) | 写给 CodeBuddy 的 skill、查其字段语义时 |

- 新软件装到本机时按同格式加文件并在本表登记；未装的软件不建空文件（Claude Code 本机未装，其字段事实只在 §4 对照表）。
- WorkBuddy 的深度细节（数据布局、跨对话通信、扩展机制全景）见第三版 `skills/workbuddy-manager/`——不入治理备份、也未随公开仓上传，按需取文件。

## 4. 平台差异速查（skill frontmatter，2026-08-22 联网核查）

写 skill 时以本表核对目标平台；治理字段（importance / platform / created_at 等）一律放 `metadata` 下——这是跨平台唯一安全位。单软件的环境状态见 §3 各文件。

| | agentskills.io 开放规范 | CodeBuddy / WorkBuddy | Claude Code | ZCode |
|---|---|---|---|---|
| 顶层字段 | name※ description※ license compatibility metadata allowed-tools | name description allowed-tools disable-model-invocation user-invocable context agent model hooks（**全非必填**，name 缺省用目录名） | 开放规范 6 字段 ＋ 同 CodeBuddy 的扩展字段 | 开放规范 6 字段 |
| 自定义字段位置 | 必须 `metadata` 下 | 无 `metadata` 字段（放了不解析，无害残留） | `metadata` 下 | `metadata` 下 |
| allowed-tools 分隔 | 空格 | 逗号 | 空格 | — |
| hooks 生效条件 | 实验性 | 仅 `context: fork` | — | — |

※ 必填。依据：agentskills.io/specification、codebuddy.ai/docs/cli/skills、code.claude.com/docs/en/skills、本机 ZCode skill 实测（document-skills:pdf 的 frontmatter 为 name＋metadata＋description＋license 结构）。

## Agent 提示词注入适配器

本节只规定机器事实的登记格式，不重新定义 `importance: 1` 的注入目标、规则内容或回退原则。具体平台能否执行注入，以及由什么脚本、命令、配置入口或手动方式执行，由当前软件文件提供。

平台适配器按以下链路解析：当前平台 / 模型 / 会话状态 → 本表对应的软件文件 → 适配器入口 → 实际验证结果 → 回退方式。当前会话只读取对应的软件文件，不能用其他软件文件的候选方法代替当前平台事实；Agent 或模型切换后重新选择并核实。

每个适配器至少登记：

- 状态：`已核实`、`未核实`、`未配置` 或 `已失效`；
- 适用的平台、模型和会话范围；
- 入口类型：脚本、命令、配置入口或手动方式；没有真实入口时明确写“无”；
- 通道穷举：列出该平台**全部已知的注入通道**，并逐一标明当前是否启用——主通道之外的次要通道（项目级配置、工作目录下的规则文件、按文件类型触发的规则等）同样要登记；不得只登记自己在用的那一条。
- 注入来源、目标、前置条件和调用方式；
- 验证方法、最近验证结果和验证时间；
- 失败时的等效常驻入口或强制首读回退。

没有经过实际核实的入口不得写成可用方法；状态为“未核实”“未配置”或“已失效”时，不得据此声称已完成系统提示词注入。

## 官方机制 → 治理接口（登记格式）

本节只规定登记格式，不复述任何平台的机制细节。用途：让「治理规则挂在哪个触发入口上」有据可查——**没有挂到任何入口的治理规则只是纸面规则**（对应 [OPERATIONS.md](OPERATIONS.md)「执行缝隙兜底」）。

每个软件文件都须有同名的一节，按本平台实际情况逐机制登记；本平台没有的机制写「不适用」，不留空、不省略。换环境时与其余机器事实同处理——不带旧值，重新采集。

机制的划分**以上位容器为准**：先列官方定义的全部子类型，再列平台自有的非插件机制；不得只登记自己用过的那几类，也不得把上位容器与子类型混成并列项。

每个机制至少登记五项：

- **官方定义文件 / 落点**：该机制在本机的定义文件或目录；
- **触发入口**：什么条件让它生效（自动注入、用户消息匹配、工具调用、手动等，按实际写）；
- **能力边界**：它做不到什么——决定它不能承接哪些规则；
- **能承接的治理规则**：本体系里哪些规则靠它执行；没有承接对象时写「无」。
- **依据等级**：登记值须标明来源等级——`[官方]`（附官方文档 URL）、`[实测]`（本机直接观测到的文件、日志或记账数据）、`[推断]`（由前两者推出、未经直接验证）；官方口径与本机实测冲突时以实测为准并记当日 Memory。没有官方依据的落点不得写成官方规范。

某条治理规则在上述入口中都找不到承接者时，按 [OPERATIONS.md](OPERATIONS.md)「执行缝隙兜底」处理，并在当日 Memory 记录。

## 迁移流程

1. 换环境第一件事：读本文件，§2 路径必须实测，不沿用旧值。
2. 软件文件逐个处理：只核对当前环境用得上的；用不上的软件文件可留旧值，下次提起来先核对再用。
3. 治理文件中出现硬编码机器信息（路径、软件安装位置、平台专属字段事实）＝违规，改为指向本文件或 §3 对应软件文件。
4. 本文件与 software 文件的旧值不归档、不迁移——到新环境重新采集。旧值的唯一去处是 git 历史（如有）。

---
title: "OPERATIONS.md - 操作规范"
summary: "所有操作行为的权威来源——创建/修改/删除、安装下载、审计、读取流程，治理文件采纳与撤销、引用关系分类和操作门禁"
importance: 1
platform: common
created_at: 2026-07-03
read_when:
  - Before any create, modify, or delete operation

---


# OPERATIONS.md - 操作规范

> 本文件是所有操作行为的权威来源，importance 为 1，与 SOUL.md 同级。

---

> **修改本文件前必须先展示修改方案并等待用户明确同意，再做编辑。只有用户针对该方案的直接肯定回复才算同意。不得在告知后自行继续。**

## 改动前告知、范围与停止

任何操作先说明对象、原因和范围；治理文件修改还须按「治理文件修改流程」取得明确同意。处理文件默认限定在所属工作文件夹内，确需越出该范围时先说明对象、理由与范围。只改用户要求的结果，不顺手清理、重构相邻系统或添加预留；任务产生的临时脚本、中间文件和调试输出在结束时清理。

需要写入文件的文本含 shell 特殊字符（`$`、反引号、引号等）时，先写成脚本文件再执行，不直接嵌进命令行——shell 先展开后执行，会静默改写内容；本机两例与规避细节见当前软件文件「运行时坑（实测）」。

指令所指的文件、字段或词在现实中找不到，或与已核实事实矛盾时，先停下确认，不猜测后编辑。以下情形同样停下：

- 非答案句被当作确认选项；
- 规则触发条件已满足，却因自行判断严重程度而不执行；
- 用户连续纠正同一产物两次或以上，表明产物形态可能不对；
- 开放式整理尚未确定单文件/多文件及原文与说明的组织方式。

触发条件只判断是否满足，不在规则外追加主观准入分级。（针对“会用情境分级替代二进制触发条件”这一缺陷而立；源事故见 MEMORY.md T20；该文件未随公开仓上传）

规则、流程和操作习惯按 [FREEDOM.md](FREEDOM.md) 选择力度；条件已经充分约束时，不另叠加同义的元层禁令。用户列举的项目是类别样例而非穷举，应识别共同特征和未覆盖的维度。

每个 Skill 和工程由各自负责的工作文件夹维护根目录 `文件夹目录.md`；完成删除检查后，待删除文件移入该负责区域的 `.trash/`。

## 文件与目录放置（按平台隔离）

治理根内的内容按"是否平台专有"决定放置，不同平台的内容不混放。本条只定放置原则；具体路径与平台名单是机器事实，见 [MACHINE.md](MACHINE.md) 及其软件文件。

- **平台无关**（通用规则、跨项目记忆、通用 skill）放扁平位置：`memory/YYYY-MM-DD.md`、`skills/<skill-name>/`；通用 skill 不套 `common/` 之类的中间层。
- **平台专有**才按平台分子目录或分文件：平台事实与机制 → `machine/<platform>.md`（由 `MACHINE.md` 管理）；平台专有记忆 → `memory/<platform>.md`（或 `memory/<platform>/`）；平台专有 skill → `skills/<platform>/`。
- 整理已有内容时若无法判断平台归属，保留在扁平位置，不强行分类。
- **回退**：删除或还原对应平台的文件 / 目录即可。

创建 / 移动文件时的具体位置判据仍按「规则先行原则」第 3 步与各文件的定位许可决定，本节只解决"平台隔离"这一个维度。

## 委派与分工

主 agent 的上下文与轮次是稀缺资源，**批量、可规格化、可验证的执行交给子代理**，主 agent 只保留判断、授权与验收。

- **该委派**（判据：规格能一次说清 ＋ 结果能客观验收）：批量文件改写、全量通读 / 检索 / 清点、跑验证脚本、相互独立的并列子任务。
- **自己留**：决定改什么与改到哪一步、需要用户裁定或涉及治理授权、对子代理产出的验收、跨任务取舍与最终交付。
- **环境坑由子代理在自己上下文里消化**：行尾差异、路径转义、断言试错这类重复劳动不该占用主 agent 的轮次——只给规格与验收口径，不问过程。
- **委派不等于放权**：子代理的回报是"它打算做什么"，不是"它做成了什么"；产出必须回原文抽验后才算完成。

本程序具体怎么委派（工具与取值）是机器事实，见 [MACHINE.md](MACHINE.md) 下当前软件文件。

## 反思与学习

以下任一条件满足时，立即加载 [reflection-evolution](skills/reflection-evolution/SKILL.md)：
- 用户纠正你的错误或指出你违反了自己的规则
- 用户要求复盘/总结教训
- 同一会话中同类错误被用户指出两次或以上

加载后走完其完整 Workflow，产出具体行动（写 memory / 修改 skill / 修改治理文件）再继续原任务。

## 记忆维护

当 memory 文件过大、信息重复、或需要将日常日志提炼为长期规则或迁出到对应治理文件时，加载 [reflection-evolution](skills/reflection-evolution/SKILL.md) 并按 Step 7 执行扫描→分析→合并→精简流程。

**记录前自检**：写入 memory 前问——这件事如果没被记下来，下次会不会漏？会 → 记；不会 → 跳过。记忆只为兜底，防止漏做可能忘的事，不记录自己自然会做的默认行为。判断标准见 [FREEDOM.md](FREEDOM.md)「记忆」。

**落点裁决**：程序自带记忆机制时，其原生记忆落点只承载**项目局部日志**（本工作区的日常记录与会话级上下文）；**跨项目与治理内容**——规则、教训、采纳与撤销四行、安装 / 下载审计、删除或移动记录、平台记忆——一律落治理根的 `memory/` 与 `MEMORY.md`，不依赖任何单个程序的原生落点。`MEMORY.md` 在本体系下的定位是**教训短记录暂存**（稳定语义，各程序复用）。各程序原生落点的具体路径属平台事实，登记在对应软件文件里，本节不复述。

## 自指一致性与规则先行

**对治理文件的任何操作——修改、审查、审计、提案——都必须先遵守该文件自身的规则。** 审查不是修改的预演，审查本身就是操作。

具体执行方式：
- 修改治理文件时：统一按「治理文件修改流程」执行；该流程负责承载修改前核对、修改后自查和冲突处理，确保拟议规则符合用户指令、目标文件现有规则和其他适用治理文件。
- 审查/审计/提案时：第一动作是对被审查的文件跑其自身所有适用约束——引用关系分类、自指一致性、改动前告知（如适用）、格式规范。审计结果必须先包含自检结论。起草涉及本治理体系的方案、计划或提案时，先按「规则先行原则」第 3 步扫各文件 summary 全图、穷举适用文件并逐项对照，再落笔——只对照想起的那几份不算完成。
- 规则之间出现冲突时，按「规则先行原则」第 4 步处理，不让新规则无说明地覆盖旧规则。
- 修改完成后，检查 skill-creator 自身的 frontmatter 和 description 是否满足其 BP #1、BP #5 等规则。

## 规则先行原则

对于可复现的问题模式，或单次但影响深远、有治理意义的改动，先立规则，后执行修改。两者都不是的日常操作不需要规则。涉及新增文件、新约定的，同时走「采纳与撤销」的分档与四行记录。

**1. 识别**：是否属于会反复出现的场景？或虽是单次、但影响深远、有治理意义？（复现≈CRR，深远≈PSU——两个触发条件恰好对应重要性的两个维度）

**判据前置**：只要正在往任何文件写入规范性内容（判断标准、禁止、应当、判据、条件），即属本流程场景——不因为形式上只是"改一个文件"就当普通编辑。

**2. 判粒度与范围**：根据任务脆弱度选择规则形式（见 [FREEDOM.md](FREEDOM.md)）；同时落范围三问——覆盖什么对象、从哪条体系原则推出、任务给的范围是推导结果还是表象。三问落纸才进下一步。

**3. 写入**：按管辖归属选择治理文件。新增、迁移、拆分、替换等一切安置决策都走本步：
- 先把内容拆成独立主张——速写、原则、态度性质不同，不按节为单位搬；
- **先问载体存废**：① 目标载体描述的对象在当前环境存在吗？② 它的每条事实在当前平台成立吗？③ 有没有同职责的载体该合并或取代？**任一落空 → 先提「删 / 合」，不要先「改」**——把内容塞进已失效的载体等于把错误制度化；
- 扫各治理文件 frontmatter 的 summary 全图（约十行；skill 的 summary 即其 description，见「文件 frontmatter 规范」），逐主张问「谁声明管这个」，不只在起冲突的文件间二选一；
- 找不到唯一管辖者才考虑新建载体（走「采纳与撤销」）；
- **改 / 删载体前先做依赖反查**：grep 它的名称与路径，查清谁引用它——否则断链不可见；
- 文件的管辖范围变更时，同步改它的 summary——失效的地图比没有地图更误导；
- 同时检查：是否需要在其他文件中添加对此规则的索引？新规则是否也适用于系统中已有但尚未修改的部分？

**4. 按规则修改**：以刚写入的治理文件为唯一权威执行修改。新规则与已有规则冲突时，冲突是对齐信号不是覆盖信号：显式改掉一方（旧或新），不在体系中留矛盾。

**5. 验证**：改完后检查所有改动是否满足新规则。

## 采纳与撤销

分档不另立标准，交给 importance 判定（见 [IMPORTANCE.md](IMPORTANCE.md)）：对改动本身问 PSU+CRR 两问——PSU、CRR 都低的小改动直接改，不走本流程；CRR 高的采纳（会产生依赖：新增文件、新约定、拆并改名等结构变动），入场前在当日 Memory 日志记四行：

- **依据**：为什么采。
- **失效信号**：什么信号出现说明采错了。列已知信号备用，但不构成穷举门槛——其他情况发现错误，同样触发更改。
- **影响面**：要动哪些文件、哪些既有规则从此多管辖一个对象。
- **依赖边界**：允许与禁止什么依赖它。被引用度即撤回成本，有意压低。

CRR 高的采纳先以参考补丁身份入场（见 [FREEDOM.md](FREEDOM.md)），试用期间冻结依赖。触发重议（失效信号出现、其他途径发现错误、长期未用）时按依据定删留；撤销时配套检查同批进行。

## Git 管理

涉及本治理体系的仓库初始化、提交、回滚、远程同步或 GitHub 推送时，遵循 [GIT.md](GIT.md)。本节只提供入口；仓库边界、具体命令和验证要求由 `GIT.md` 统一管理。

## 能力类问题核实

被问到 WorkBuddy 自身的能力、功能、机制时，先查官方文档（codebuddy.cn/docs/workbuddy）核实再答，不要仅凭通用推理作答。

## 核查、覆盖度与可用项

开放式检查、清理、审查、梳理或对比开始时，先明确：判定标准、检查维度、边界和不应触碰的内容。凡验证、检查、差异核对或结构修改，都以实际穷举为准，不用记忆列举：

- 章节号、重命名或重排完成后，穷举所有引用逐一核对。
- 差异或对齐任务开始前，列出基线、目标和中间产物，逐对比较。
- 报告"已全部""无缺失""已对齐"前，列出实际纳入检查的对象。
- 追加测试前先问是否会产生新的信息；没有就不做。

判定缺少工具、文件、任务或方案之前，列出并检查全部可用来源：

- 工具：PATH、常见及非标准安装目录、包管理器清单。
- 任务或文件：自动化、会话、本地文件和已安装 skill。
- 搜索或下载：网络、平台市场、平台仓库和本地参考材料。

没有完成相应的全量检查前，不把默认路径未找到推断成系统中不存在。

## 并发修改纪律（元规则）

治理文件可能被多个会话同时修改：

- 编辑前重新读取目标文件当前完整内容，不凭旧报告或记忆编辑。
- 每次写入后立即读回或 grep 关键段，确认内容生效且未被覆盖；发现冲突就停下报告。
- 重大治理改动按 [GIT.md](GIT.md) 通过提交和远程推送保留可追溯记录，不另行保存日期快照。
- 文件归属不明或已有其他会话在修改时，不重复覆盖，先补充或对齐目标。

## 执行缝隙兜底（元规则）

标为"必须"或"不可跳过"的规则，应有明确的执行时机和负责载体。能由脚本、hook、automation 或 checklist 检查的，优先落到对应机制；没有执行机制的内容不伪装成已经自动生效的硬门槛。治理体检由 `system-refactor` 编排，机械一致性检查可用 `scripts/governance_lint.py` 辅助；脚本本身不替代人工裁决。

需要长期承担的治理动作必须有明确 owner，不把"有人应该检查"当成职责分配。

## 校准节奏（人机反馈闭环）

以下情况需要主动向用户对齐，不自行消化：

- 同类纠正连续达到三次。
- 新增会改变行为取向的规则。
- 新建、改名、拆分或改变 importance 等结构性治理改动。
- 发现多重矛盾或内容归属纠葛。

流程细节、措辞和格式可以直接处理；取向性、结构性和矛盾性变化应交用户判断。

## 治理体检（周期性自检）

日常验证、单文件检查和机械 lint 与 `system-refactor` 的整体体检不同：前者按各自流程执行，不会自动触发整体体检。只有用户明确要求对治理系统进行整体体检或审计时，才使用 `system-refactor` 的只读流程；发现异常或连续多轮改动时可以提出体检建议，但不据此自行启动整体体检。

1. 检查引用关系分类、断链、frontmatter 和名单与实际文件集的一致性。
2. 检查同一规则是否在多个文件重复定义，按归属收敛到唯一权威来源。
3. 检查 importance 是否与实际复用和影响面相称。
4. 检查规则是否过载；达到记不全或改一处牵动过多文件时，优先精简而不是继续叠加。

体检结果写入当日 Memory；异常项列出后再决定修复，不把体检本身变成未经授权的修改。

## 定位已有任务或文件

被要求找或回复已有任务/文件时，先主动用工具查找（automation_update 列自动化、resume 列会话、Grep 搜工作区与当前平台已核实的对应软件工作目录，位置按 [MACHINE.md](MACHINE.md) 及其软件文件核对），确认确实找不到再开口。不反问"在哪/叫什么"。

## 治理文件修改流程

修改或新建治理文件时的流程指引——只保证流程合规，不提供编写规则（怎么写遵循治理文件自身规则，见「文件 frontmatter 规范」）。属于规则先行场景的改动（可复现或影响深远），先按「规则先行原则」立规则，再走本流程执行修改；两流程重叠的写入与验证步骤合并执行一次，不重复。

1. **展示方案**：修改前先展示方案——改什么、为什么、怎么改，等用户明确同意再做（确认标准见本文件顶部）。
2. **读终态与前置核对**：第一个工具调用是 Read 目标文件，也检查是否被并发会话改动过；随后按「自指一致性」完成修改前核对。
3. **编写**：按治理文件自身规则写——本流程不干预编写内容。
4. **自指与冲突**：按「自指一致性」改完立即自查；违反或冲突则先修正，或请求用户决定，再声明完成。
5. **验证**：grep 穷举残留（旧词、旧章节名、旧标题）；读回终态确认落位。
6. **记录**：到当日 Memory 日志。
7. **层级审计**：修改本文件或 SOUL.md 后，检查当前平台用户级 `USER.md` 的沟通偏好节（该文件未随公开仓上传；位置按 [MACHINE.md](MACHINE.md) 及其软件文件核对）——① 高层文件新增了什么原则？② 沟通偏好里有没有等效表述？有则重复，删；③ 沟通偏好里有没有操作行为类规则应升入 OPERATIONS.md？有则升迁，删原条目。归类标准：操作行为约束 → OPERATIONS.md；人格准则 → SOUL.md；纯表达风格 / 沟通偏好 → USER.md。

新建治理文件时额外走：**采纳与撤销**四行记录（见「采纳与撤销」）、登记 [README.md](README.md) 名单、frontmatter 按「文件 frontmatter 规范」写。

---

## 1. Importance 分级

所有文件统一使用 PSU（问题解决帮助度）+ CRR（跨资源复用度）双维度评估，1~10，越低越重要。详见 [IMPORTANCE.md](IMPORTANCE.md)。

---

## 文件 frontmatter 规范

根治理文件 frontmatter 必含 `title` 与 `summary`。summary 写「身份＋管辖范围」，口号式不算；定位是文件自身的元数据——根文件是否被平台读取或注入，取决于已核实的当前平台、模型和会话实际加载结果，不得仅凭文件存在或 frontmatter 推断已注入；frontmatter 供 lint、索引、迁移工具读取，读前导航不依赖它。文件定位只此一处，正文不再写职责行。

skill 的描述字段是 `description`（官方注入、触发匹配靠它），书写规范见 [skill-creator](skills/skill-creator/SKILL.md) BP #1；治理字段位置与平台差异见 [MACHINE.md](MACHINE.md) §4。

**治理文件的编写规则先于 skill-creator**：根治理文件不是 skill——skill-creator 的流程与 BP（description 触发词、打包、渐进披露等）只适用于 skill，不适用于根治理文件。治理文件遵循自身规则（本节 frontmatter 规范、[FREEDOM.md](FREEDOM.md) 写作检查、例子防误导、减少优先）；两者冲突时以治理文件规则为准。

---

## 2. Skill 治理

Skill 分两类：**治理型**（如 skill-creator、reflection-evolution）是带触发条件的治理文件；**工具型**（如 pdf-text-extractor）是外挂式领域工具，不承载治理职能。分类判据与治理文件名单见 [README.md](README.md)；新建/删除治理文件时同步更新名单，分类存疑时按其判据裁定并记入当日 Memory。

创建、修改、验证 skill 的全流程遵循 [skill-creator](skills/skill-creator/SKILL.md) 及其 Best Practices。本工作区额外强制项：

- importance 按 [IMPORTANCE.md](IMPORTANCE.md) 评估定值；引用关系分类与采纳与撤销规则同等适用。
- 创建或修改后到当日 Memory 记录。

---

## 3. 引用关系分类

文件之间的引用按链接实际承载的语义分三类：
- **必要引用**（如"判断标准见 `xxx`"）：被提及文件是本文件的权威规则来源或必要依赖——本文件的行为受其规则约束，或本流程的核心步骤依赖它。应能说明依赖的具体内容和用途。
- **提示**（如"提示：可用 `xxx` skill 做 Y"）：仅作为**可选建议**——AI 可自行决定是否加载，不用也走得通。**不构成依赖**。
- **导航 / 定位 / 事实索引**（如目录、地图、元数据说明"本文件是参考补丁，见 `xxx`"）：只为指引读者去哪找。**不构成依赖**。

`importance` 不决定谁可以引用谁，也不自动决定谁拥有管辖权。权威关系由文件的实际职责、内容归属和依赖关系判断；引用本身也不赋予引用方对被引用方的管辖权。此分类不仅适用于 Skill，也适用于 OPERATIONS.md、SOUL.md 等治理文件对外部文件的引用。

### 如何区分必要引用、提示与导航

正文中提及另一个文件或 Skill 时，按去掉该链接后的实际影响判断：

- 去掉链接后核心步骤不完整 → 属于**必要引用**，应保留并说明依赖内容。
- 去掉链接后核心步骤仍完整，只是提供可选路径 → 属于**提示**，不构成强制依赖。
- 链接只负责告诉读者文件在哪里、记录了什么或从哪查 → 属于**导航 / 定位 / 事实索引**，不把被指向文件写成权威来源。

该分类说明链接的作用，不改变被引用文件本身的权威性，也不改变任何操作门禁。

---

## 4. 废弃流程

1. 在文件 frontmatter 加 `deprecated: true`
2. 检查是否有其他文件引用此文件
3. 如有引用，更新引用方指向替代文件
4. 保留文件不删除，等待确认后进入删除流程

---

## 5. 删除流程

1. 检查是否有其他文件引用此文件
2. 无依赖且不重要 → 移入工作文件夹内的 `.trash/`，不做永久删除，到当日 Memory 记录
3. 其他情况，或不确定 / 有疑虑 → 告知用户，获确认后删除再记录

工作文件夹内的删除统一采用可恢复的移动方式：只移入工作文件夹内已有或按需新建的 `.trash/`，不执行永久删除命令。第 3 步获得确认后，也仍按该移动方式处理；无法确定 `.trash/` 位置或移动范围时先停下确认。

---

## 6. 安装/下载流程

**每次安装/下载前必须先读本节，不可跳过。**

### 四步流程

1. **告知**：告诉用户要安装什么、从哪来、为什么需要
2. **安全判断**：评估来源可信度、依赖链、风险等级
3. **执行**：执行安装/下载
4. **审计记录**：到当日 Memory 记录安装操作（时间、包名、来源、依赖链、验证结果）

### 注意事项

- 无论用户回复"随便你"还是其他，四步全部完成
- "随便你"只影响告知环节的详细程度，不影响其他步骤
- 内置 Skill 涉及外部服务调用或文件下载时仍需走本节四步流程

---

## 7. 文件拆分

当一个文件中某一节是独立可自洽的体系时，应拆分到单独文件（"细节外包模式"：主文件只保留链接与约束，细节下放到独立文件）。

涉及压缩、拆分或合并时，先按 [IMPORTANCE.md](IMPORTANCE.md) 的内容保留判断列出必须保留的定义、边界、权威关系、路由条件和验证条件；修改后逐项核对这些语义是否仍然存在。完成核对前，不得仅因篇幅长、内容不直接执行或使用频率低而删除。

---

## 8. 文件读取审计规范

每次处理文件集时，如实标注每个文件的读取状态：FULL READ（已读全部）、PARTIAL READ（只读了部分）、NOT READ（只读了文件名/列表）、SCANNED（扫描件无法提取文字）。读取失败必须记录原因，不得虚报。

审计原则见 [FILE-AUDIT-RECORDS.md](FILE-AUDIT-RECORDS.md)。

---

_本文件是操作规范的权威来源。如需修改，先展示方案并等待用户明确同意（见文件顶部）。_

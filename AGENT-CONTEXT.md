---
title: "AGENT-CONTEXT.md - Agent 提示词与上下文工作规范"
summary: "Agent 提示词与上下文工作的权威来源——治理文件选择、加载时机、内容形态（描述侧/应用侧）、注入通道判定、上下文预算、压缩恢复和真实性校验"
importance: 2
platform: common
created_at: 2026-09-02
read_when:
  - Before designing or changing Agent system prompt injection
  - When deciding governance file loading, context compression, or recovery
---


# AGENT-CONTEXT.md - Agent 提示词与上下文工作规范

> 本文件定义 Agent 提示词和上下文工作的跨平台规则，不记录具体平台的注入接口、版本或机器路径。

## 1. 权威与平台联动

- 本文件负责文件选择、加载分层、上下文预算、压缩保留、压缩后恢复和真实性校验；创建、修改、删除、确认和停止门禁仍由 `OPERATIONS.md` 管辖，重要性评分由 `IMPORTANCE.md` 管辖。
- 完整治理文件是唯一权威原文。系统提示词中的规则核、摘要、压缩稿或入口都是派生材料，不得形成另一套规则。
- `importance: 1` 文件按预期进入系统提示词直接注入层，但只注入当前生效、稳定且必须常驻的规则核心；日志、历史、例子、机器快照和大段背景按需读取。
- **注入 / 常驻是多通道的，且不限于治理文件**：判断「某段内容会不会进上下文、会不会改变每次注入」，必须按当前平台**全部已知通道**核对——非治理内容同样可能常驻（如工具定义、技能描述、平台自带的提醒块等）；**不得由「是否 `importance: 1`」或「是否属治理文件」推断**，也不得由「某文件不可注入」推断「其中没有内容常驻」。通道清单由当前软件文件逐一登记（格式要求见 [MACHINE.md](MACHINE.md)「Agent 提示词注入适配器」），本节只定原则、不列清单。
- 实际链路为：当前平台 / 模型 / 会话状态 → [MACHINE.md](MACHINE.md) → 当前软件文件 → 已登记的注入适配器 → 实际验证结果 → 等效常驻入口或强制首读回退。只读取当前软件文件；适配器未核实、未配置或已失效时，不得把预期路由当成已执行结果。
- 平台不支持直接注入时，使用已核实的等效常驻入口；不存在时回退为强制首读。回退不等于直接注入，未验证实际结果时不得声称“已注入”。Markdown 文件存在也不等于已读、已注入或会自动执行。

## 2. 加载时机与文件选择

默认必读的权威文件是 `OPERATIONS.md`、`FREEDOM.md` 和 `IMPORTANCE.md`；除非已核实当前平台、模型和会话已经完整加载其原文，否则启动工作前读取原文。其他文件按任务范围、`read_when` 和实际决策需要选择。

按以下层次选择读取方式：

- **直接注入 / 常驻层**：按预期承载 `importance: 1` 文件的稳定规则核心，不等同于全文注入；**常驻内容不限于治理文件**（见 §1）。
- **强制首读层**：平台没有自动注入时，启动或进入相关工作前必须读取的权威文件。
- **按需层**：由任务类型、已加载规则或当前决策需要引入的文件。
- **触发层**：满足文件 `read_when`、任务场景或明确条件时加载。
- **特定场景层**：对应平台、工具、技能或问题实际涉及时读取。

需要规则时读取权威原文，不以摘要替代；文件选择解决“读哪些”，上下文压缩解决“在预算内保留什么”，不得用压缩替代必要读取。

平台实际的注入范围与读取入口因程序而异，**每个程序须在其软件文件登记该程序的必读清单与读取层级**（只登记实际会读的文件与入口，读取规则仍以本节为唯一权威，不按清单另立一套）；注入入口的登记字段见 [MACHINE.md](MACHINE.md) 的「Agent 提示词注入适配器」。

### 描述侧与应用侧

同一内容按用途分两侧，投递方式随之不同。**描述侧**供理解，可以给足，落在按需层也不影响使用；**应用侧**要求被执行者在运行时用上——若没有任何触发使它被想起，这一侧就不存在。

因此应用侧只写概括的方向，不写操作守则：写实了会绑住强执行者、也帮不到弱执行者，还把「怎么应用」这一判断提前做死；**其详略按执行者的能力定**，可落地的判断留给运行时。内容该不该写、写到什么力度，仍按 [FREEDOM.md](FREEDOM.md) 判断；本节只管它以什么形态投递、靠什么被读到。

## 3. 上下文预算与压缩

上下文预算优先保障用户最新目标与边界、当前任务所需的权威规则、已确认事项、执行状态和恢复事实。预算不足时，依次压缩或下放背景、推导、历史、非必要例子、重复表述、无关机器事实和低重要性参考材料；不得为保留全文挤占规则核，也不得仅因成本高降低文件重要性。

任何压缩都必须保留触发条件、必须动作、禁止事项、停止 / 确认条件、例外边界和权威关系；不得把“必须”压成“建议”，或把二进制触发条件改成模糊分级。

## 4. 压缩后恢复

接近上下文上限、发生平台自动压缩或跨会话交接时，最小恢复摘要至少记录：用户最新目标、范围和边界；已确认、已拒绝、待确认事项和未解决冲突；已读文件及读取状态；已完成/未完成动作、未验证假设和下一步；文件权威关系、当前来源及高影响事实。

恢复后依次：

1. 对照用户最新指令和摘要，确认目标、范围和状态未漂移。
2. 对高影响操作回读权威原文，核对触发条件、例外和停止 / 确认门禁。
3. 摘要与用户指令冲突时以用户指令为准；与权威原文冲突时以原文为准。
4. 不得仅凭摘要声称文件已完整读取、操作已完成或事实已验证。

恢复摘要只是导航和恢复材料，不是治理规则的新权威来源。

## 5. 机制或 Agent 切换

注入方式、加载入口、上下文处理机制、平台、模型或 Agent 发生实际变化时，重新沿本文件的链路读取对应 `MACHINE.md` 软件文件并核实适配器结果；只有稳定原则、重要性评分或默认优先级改变时才修改对应治理文件。新增规则、文件或依赖按治理文件的采纳与撤销要求记入当日 Memory。

---

_本文件是 Agent 提示词与上下文工作的权威来源；具体平台机制以对应机器事实文件为准。_

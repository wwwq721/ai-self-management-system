# 文件夹目录

本文件是治理根工作文件夹（本文件所在目录）的标准目录记录。

## 记录规则

- 只记录本文件所在工作文件夹及其下级目录，路径统一使用相对路径。
- 不记录机器绝对路径、平台用户目录或运行时临时路径。
- 目录或文件发生新建、移动、改名、删除时，完成操作后立即同步本文件。
- 各 Skill 工作文件夹分别维护自己的 `文件夹目录.md`，本文件只记录工程级目录和各子目录归属。
- 完成删除检查后，待删除文件移入负责区域的 `.trash/`；该目录按需创建，并在目录发生变化后登记。

## 标准目录

| 相对路径 | 用途 | 必需/可选 | 当前状态 | 使用时机 |
| --- | --- | --- | --- | --- |
| `AGENT-CONTEXT.md` | Agent 上下文稳定原则及切换状态备注 | 必需 | 当前存在 | 处理 Agent 上下文加载、压缩或恢复时 |
| `FREEDOM.md` | 自由度与规则力度判断 | 必需 | 当前存在 | 写规则、流程或约束时 |
| `FUTURE.md` | 对未来版本的预言 | 必需 | 当前存在 | 查看体系未来可能的样子时 |
| `GIT.md` | 治理体系版本控制规范 | 必需 | 当前存在 | 初始化、提交、回滚或远程同步前 |
| `IDENTITY.md` | Agent 身份事实和表达速写；**不入公开仓**（工作仓跟踪），示例见 `examples/IDENTITY.example.md` | 必需 | 本机存在（不入公开仓） | 确认 Agent 身份时 |
| `IMPORTANCE.md` | 重要性分级和注意力路由 | 必需 | 当前存在 | 评估文件或内容重要性时 |
| `MACHINE.md` | 机器、平台和软件事实的总入口 | 必需 | 当前存在 | 核对平台能力或软件事实时 |
| `MEMORY.md` | 项目记忆索引和记录边界；**不入公开仓**（工作仓跟踪），示例见 `examples/MEMORY.example.md` | 必需 | 本机存在（不入公开仓） | 定位项目记忆时 |
| `OPERATIONS.md` | 操作行为、治理文件修改和操作门禁 | 必需 | 当前存在 | 创建、修改、删除或审计前 |
| `SOUL.md` | Agent 人格原则 | 必需 | 当前存在 | 确认人格边界时 |
| `README.md` | 治理文件判据和名单 | 必需 | 当前存在 | 判断治理成员范围时 |
| `USER.md` | 用户档案、隐私偏好和沟通偏好；**不入公开仓**（工作仓跟踪），示例见 `examples/USER.example.md` | 必需 | 本机存在（不入公开仓） | 处理用户偏好或档案时 |
| `文件夹目录.md` | 记录本工程工作文件夹的标准目录结构和变更约定 | 必需 | 当前存在 | 修改工程目录、文件、脚本或资源前后 |
| `.git/` | 本工程 Git 仓库的版本元数据 | 必需 | 当前存在 | 执行 Git 版本控制操作时 |
| `.gitignore` | Git 纳入与排除边界 | 必需 | 当前存在 | 暂存、提交或核对仓库边界时 |
| `.governance-backups/` | 历史遗留备份的临时存放区；后续追溯以 Git 提交和远程推送为准 | 可选 | 治理根未创建（历史备份留在已冻结的桌面副本） | 清理历史备份时 |
| `machine/` | 各平台和软件的机器事实文件 | 可选 | 当前存在 | 核对具体平台或软件事实时 |
| `memory/` | 项目每日记忆与长期记忆 | 可选 | 当前存在 | 记录或检索项目经验时 |
| `scripts/` | 工程级重复检查脚本 | 可选 | 当前存在 | 执行机械治理检查时 |
| `scripts/governance_lint.py` | 治理文件结构、链接、锚点和名单巡检 | 可选 | 当前存在 | 运行治理机械巡检时 |
| `examples/` | 个人档案与身份文件的样例模板（真值不入公开仓） | 必需 | 当前存在 | 自建 `USER.md` / `MEMORY.md` / `IDENTITY.md` 时 |
| `skills/` | 工程使用的 Skill 集合；各 Skill 维护自己的目录记录 | 可选 | 当前存在 | 选择、维护或验证 Skill 时 |
| `skills/.git/` | 工具型 Skill 独立仓库的版本元数据（治理型 Skill 仍归本仓，见 [GIT.md](GIT.md) §1） | 可选 | 当前存在 | 对工具型 Skill 执行 Git 操作时 |
| `skills/.gitignore` | 该仓库的纳入与排除边界 | 可选 | 当前存在 | 暂存或核对工具型 Skill 边界时 |
| `skills/.gitattributes` | 该仓库的行尾与二进制资源归属规范 | 可选 | 当前存在 | 处理跨平台行尾或二进制资源时 |
| `skills/*/文件夹目录.md` | 各 Skill 工作文件夹的独立目录记录 | 必需 | 当前存在 | 修改对应 Skill 目录或资源时 |
| `.trash/` | 暂存完成删除检查后移出的工程级待删除文件 | 按需 | 当前存在 | 删除工程级文件且需保留可恢复副本时 |

## 维护边界

本文件只描述工程级目录、文件归属及使用时机，不替代治理文件自身规则、机器事实文件、Memory 记录或各 Skill 的行为规则与目录记录。目录清单与实际结构不一致时，以实际检查结果为准并立即修订本文件。

---
title: "IMPORTANCE.md - 重要性分级体系"
summary: "说明如何依据系统与文件目的，用 PSU+CRR 评估重要性并安排注意力、核查、验证与传递"
importance: 2
platform: common
created_at: 2026-07-03
---

# IMPORTANCE.md - 重要性分级体系

> 定义所有文件的重要性分级。被 SOUL.md 和 OPERATIONS.md 引用。
>
> 所有文件类型统一使用 PSU + CRR 双维度评估，1~10，越低越重要。

---

## 目的先于重要性

重要性判断必须服务于被评文件及当前任务的目的，不能脱离目的看文件名、篇幅、复用数量或格式。frontmatter 的 `summary`（Skill 为 `description`）是定位的压缩入口，完整正文仍是权威来源；定位、摘要与正文职责不一致时，先修正归属或说明，不用数值掩盖目的不清。

PSU 衡量文件对达成系统或任务目的的帮助与保障程度，CRR 衡量复用范围及错误或缺失的影响面。重要性是文件级信号；执行点和文件内部仍需分别判断重点，复用度扩大影响面但不赋予管辖权。

importance 也标记内容是否值得带向未来：低值（重要）表示稳定、需要传递的核心，高值（弱）表示迁移时可舍弃的内容；实际取舍仍按内容作用和当前环境核对。

---

## 1. 评分维度

### PSU — 问题解决帮助度（Problem-Solving Utility）

该文件在多大程度上改善或保障你达成目标的能力。具体问：

- 缺少此文件时，操作质量或安全性会下降多少？
- 有此文件时，决策正确性、效率、或安全性提升了多少？

### CRR — 跨资源复用度（Cross-Resource Reuse）

其他文件或系统组件在运行时有多大程度依赖于此文件的存在和正确性。具体问：

- 有多少其他文件会在执行过程中读取、调用、或受此文件约束？
- 此文件一旦出错或不存在，影响面有多大？

## 2. 评分表（1~10，越低越重要）

| importance | PSU | CRR | 含义 |
|:--:|------|------|------|
| 1 | 极高：根本性影响达成目标的能力 | 极高：几乎所有文件都可能引用 | 基础层；按预期直接注入系统提示词 |
| 2 | 高：显著提升或保障 | 高：多个文件会引用 | 核心层 |
| 3 | 较高：明显帮助 | 较高：经常被引用 | 重要层 |
| 4 | 中高：有一定帮助 | 中：偶尔被引用 | 有用层 |
| 5 | 中：特定场景有帮助 | 低：很少被其他文件引用 | 场景层（**默认值**） |
| 6 | 低：帮助有限 | 极低 | 辅助层 |
| 7~10 | 递减 | 几乎不被复用 | 极少使用 |

## 3. 默认值

新建文件未指定 importance 时，默认值 = **5**。

## 4. 评估要求

- 创建或修改文件时必须用 PSU + CRR 两个维度评估，不可不经评估直接填默认值 5
- 评估理由写入当日 Memory 日志
- 默认合成规则：`importance = min(PSU 分值, CRR 分值)`；数值越低越重要，因此任一维度更重要时，整体重要性不得被另一维度的较高数值抵消
- 若文件同时具有高复用度和高上下文成本，优先压缩正文、拆出按需细节，不得仅为保留全文而把它直接塞入常驻层

### 内容保留与压缩判断

文件内部不另设数字分级。压缩、拆分或迁移前问：删除或迁移该内容，是否会改变对象定义、理解、决策、执行、适用边界或验证结果？会则保留其语义，可改写或迁移；只有不会改变这些结果时才允许删除。低频或条件性使用不等于低重要，放入按条件读取的位置时仍保留明确读取条件。（“低频或条件性使用不等于低重要”这句是针对“拿重要性或频率当删除依据”这一误用立的；源事故见 MEMORY.md T28；该文件未随公开仓上传）

## 5. importance 与注意力优先级

### importance=1 的直接注入预期

`importance: 1` 是系统提示词直接注入的预期资格，不等于全文注入或已经完成注入；只注入稳定且必须常驻的规则核心，完整文件仍是唯一权威原文。实际注入、等效常驻入口、强制首读和验证方式按 [AGENT-CONTEXT.md](AGENT-CONTEXT.md) 及当前平台事实执行；未核实结果时不得声称已注入。其他 importance 数值本身不决定固定加载方式。

---

_此文件定义分级体系本身。如需修改分级规则，告知用户。_

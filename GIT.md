---
title: "GIT.md - 版本控制规范"
summary: "治理体系版本控制规范——仓库边界、提交、回滚、远程同步与 GitHub 认证"
importance: 3
platform: common
created_at: 2026-09-06
read_when:
  - 初始化或检查治理体系 Git 仓库之前
  - 提交、回滚、设置远程仓库或推送之前
---

# GIT.md - 版本控制规范

> 本文件是治理体系 Git 的**规范与边界**权威（仓库边界、提交与回滚规则、远程与可见性约束）；**具体命令与执行步骤下放在 `git-workflow` skill**（工具型 Skill，归另一个私有仓，未随公开仓上传），本文件不复述。一般文件操作门禁仍由 [OPERATIONS.md](OPERATIONS.md) 负责。

## 1. 管理对象与边界

本仓库只管理当前治理体系的可复用文件，默认对应个人账号下的私有 GitHub 仓库；不把运行时缓存、历史备份或日常记忆日志当作治理体系版本内容。**`machine/` 是治理文件 [MACHINE.md](MACHINE.md) 所依赖的文件夹**，属治理体系版本内容；其中记录的机器事实仍按机器事实的性质维护——换环境时重新采集，不把旧值当有效值沿用。

纳入版本控制的内容：

- 根目录已登记的治理文件，**含 `USER.md`、`MEMORY.md`、`IDENTITY.md` 三份个人档案与身份文件**——这三份由工作仓（私有）跟踪，才有版本历史与备份；派生公开仓不含它们（见下）；
- 根目录 `文件夹目录.md` 等治理体系的工程级管理记录；
- [README.md](README.md) 登记的治理型 Skill 及其实际工作文件；
- `machine/` 下各平台与软件的机器事实文件——[MACHINE.md](MACHINE.md) 依赖的文件夹；
- 治理体系使用的检查脚本和必要的仓库配置文件。

排除版本控制的内容：

- `memory/` 下的日期日志和软件记忆日志——**排除主因是私密性较强**：内容含个人上下文、判断过程与个人事实（判据见 `USER.md` 的隐私偏好节；该文件未随公开仓上传），且 §4 已声明 Git 历史不保证机密一旦提交后能够安全移除；
- 未登记为治理型 Skill 的工具型 Skill；
- `.governance-backups/` 下的临时快照；
- `__pycache__/`、构建产物、调试输出、凭据和其他运行时临时文件。

工具型 Skill 的版本控制由**另一个独立私有仓库**承担，本机路径即 `skills/`，远程 `https://github.com/wwwq721/skill.git`（2026-09-20 建立并完成首次推送，默认分支 `main`，可见性为私有）；该仓自行排除治理型 Skill、凭据、市场元数据与平台产物。因此上面的排除项只表示「不属本仓纳管范围」，不表示这些文件没有版本控制——两个仓库的边界各自独立核对，本仓不代为声明另一仓的状态。

**派生公开仓**：`https://github.com/wwwq721/ai-self-management-system`（公开；2026-09-22 从工作仓派生建立）——它从本仓派生：内容取自本仓当前版本，但**剥掉 `USER.md`、`MEMORY.md`、`IDENTITY.md` 三份**，改以 `examples/` 下的样例模板代替；**只在用户明确指令时更新**，不随本仓提交自动推送，推它之前照样先过 §2 的私密性审核。**发布路线固定为「只上传当前状态、并入云端线路、不上传本地历史版本」**：每次都以本仓当时的当前版本取一份文件快照，接在公开仓云端已有的历史之后；本仓的历史提交——含三份个人档案被剔除之前的旧版本——一律不上传，因此不直接推本仓、也不把本仓 `.git` 带过去（原因：本仓历史里存过那三份真值，带上历史就白剥了）。执行步骤见 `git-workflow` skill 的「派生快照仓的发布」（该 skill 未随公开仓上传）。**副本里凡引用到未随公开仓上传的文件之处，保留原引用文字并就地备注「未随公开仓上传」，不改写成其他指向、也不删掉引用。**判据是这处引用是否在指引读者去找那份文件（「见 / 参 / 判据见 / 格式见」这类寻址引用，以及目录表里的状态列）；只是把文件名当事实叙述的不加备注，免得满篇噪声。所以排除项与本仓名单只描述本仓边界，不等于派生仓的边界。

实际边界以本文件、[README.md](README.md) 和仓库 `.gitignore` 共同核对；三者不一致时，先暂停提交并按 [OPERATIONS.md](OPERATIONS.md) 的规则收敛，不用一次提交掩盖边界冲突。

## 2. Git 操作前检查

执行初始化、提交、回滚或远程同步前，逐项确认：当前目录是本治理体系仓库根目录，且已读本文件与 [OPERATIONS.md](OPERATIONS.md) 的适用规则；分支、工作区、暂存区、远程地址与 `.gitignore` 均符合预期；用实际文件集核对纳入范围，特别确认 `memory/`、备份和缓存**未被追踪**；待提交内容没有密码、访问令牌、私钥、个人隐私或第三方不愿公开的内容；适用的治理检查已完成。

> **推送前私密性审核**——**只在推送公开仓时做；私有仓不适用**（用户 2026-09-26 裁定："git 私有仓库不需要私密性审查"）：把将上传的文件集当成别人能读来审，看是否含可唯一识别用户本人的信息、凭据（密码、令牌、私钥、`.env`）或第三方不愿公开的内容；**本机用户名与本机路径不计入**（2026-09-21 用户裁定）。命中就停下问用户，不自行脱敏后照传；步骤见 `git-workflow` skill 的「上传前私密性审核」（该 skill 未随公开仓上传）。

遇到 `dubious ownership`，只对报错中的精确路径做一次性 `safe.directory`——**不把宽泛目录加入全局信任**，也不为绕过保护而操作未知仓库。命令与排查步骤见 `git-workflow` skill（未随公开仓上传）。

Git 检查只能证明版本控制状态，不能代替治理体系自检、Skill 验证或文件读取审计。

## 3. 提交规则

- 一次逻辑改动对应一个提交；同批配套规则、脚本和验证结果一起提交，不留下只改一半的配套检查。
- 提交前查看 `git diff` 和 `git diff --cached`，确认文件边界、内容和删除/移动操作都符合用户要求。
- 提交信息说明改动对象和结果；治理改动同时写明其依据或目的。
- 初次建库使用一个可识别的基线提交；后续改动不要把无关格式化、机器快照或日志混入同一提交。
- 未经用户明确要求，不改写共享历史，不强制推送，不用 `reset --hard` 丢弃未提交内容。
- 治理文件或其配套规则验证通过后，默认将本次逻辑改动提交并推送当前明确分支；不另行创建日期快照。

## 4. 回滚与删除

回滚前先查看 `git log`、目标提交和当前工作区状态，区分“撤销已提交改动”和“保留未提交改动”。默认优先用新的反向提交保留审计轨迹；需要恢复单个文件时也先确认目标版本和影响范围。

文件删除仍遵循 [OPERATIONS.md](OPERATIONS.md) 的可恢复删除流程：完成删除检查后移入负责区域的 `.trash/`，不以 Git 删除代替文件回收。Git 历史是可恢复的版本记录，但不是删除落点——它按已提交的版本留痕，覆盖不到未跟踪的文件，也不保证机密一旦提交后能够安全移除。

## 5. 远程仓库与 GitHub

远程仓库地址必须来自用户提供、当前仓库已有配置或已核实的本地项目事实；没有地址时不猜仓库名、不代建远程仓库。

治理体系仓库默认创建为私有仓库。改为公开或创建公开仓库必须得到用户明确授权；“上传到 GitHub”本身不等于授权公开。

设置或使用远程仓库时的约束：

- 首次推送前确认远程仓库确实对应本治理体系（核对远程 URL、目标分支与当前提交）。
- 凭据一律交给本机已配置的认证工具或平台认证流程处理；令牌、密码和私钥不写入 Markdown、脚本、提交信息或命令输出，复用凭据时同样不提取、不记录令牌内容。本机凭据管理器的可用状态属机器事实，见 [MACHINE.md](MACHINE.md) §3 下当前软件的软件文件。
- 先用只读方式核对远程可达性和权限，再推送；推送失败时保留本地提交并记录失败原因，不反复盲试。
- 默认推送当前明确的分支，不强制推送；远程已有内容时先比较历史，避免覆盖未知工作。
- 推送后核对远程分支、提交哈希和工作区状态，确认本地与远程结果一致。

本机没有 GitHub CLI 或未配置远程地址时，仍可完成本地初始化和提交；“已上传 GitHub”只有在远程地址、私有状态、认证和推送回执都核实后才能声明。步骤见 `git-workflow` skill（未随公开仓上传）。

## 6. 本仓库的最小验证

提交前后至少核对：工作区与暂存区状态；本次改动的文件清单与内容；纳入范围（`memory/`、备份和缓存**未被追踪**，`machine/` 已纳入，排除目录的忽略规则符合预期）；治理 lint、相关 Skill 验证与脚本语法检查结果；提交后的提交哈希、当前分支与工作区状态。

验证结果只报告实际执行过的命令和结果，不把本地提交写成远程上传，也不把远程地址存在写成推送成功。对应命令见 `git-workflow` skill（未随公开仓上传）。

